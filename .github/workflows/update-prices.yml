name: Update prices

on:
  schedule:
    # Noon Prague, weekdays. Cron is UTC and has no notion of DST, so this
    # is 12:00 during CEST (UTC+2) and 11:00 during CET (UTC+1) — switch to
    # '0 11 * * 1-5' at the October changeover to hold noon through winter.
    - cron: '0 10 * * 1-5'
  workflow_dispatch: # allow manual trigger

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install yfinance pytz

      - name: Fetch prices and update history.js
        run: python scripts/update_prices.py

      - name: Commit and push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add history.js
          git diff --staged --quiet || git commit -m "chore: update prices $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
